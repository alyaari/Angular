/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { SumPipe } from './sum.pipe';

describe('Pipe: Sume', () => {
  it('create an instance', () => {
    let pipe = new SumPipe();
    expect(pipe).toBeTruthy();
  });
});
